export class HeaderItem {
    public title: string;
    public route: string;
    public icon: string;
    
    constructor(title: string, route: string, icon: string) {
        this.title = title;
        this.route = route;
        this.icon = icon;
    }
}